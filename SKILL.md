---
name: ppt-production
description: 门控式 PPT 制作流程规划 skill（纯规划，不直接制作文件）：先确认 PPT 主题对象（游戏/小说/产品等）并澄清用途、受众、页数等需求；用户须提供美术资料（一张主视觉 + 数张核心人物立绘），用视觉分析提取美术风格（色彩/画风/版式/字体气质）并确认；产出大纲（含叙事主线与视觉规范）与制作规格，委托外部能力制作验证页与成品，并对照需求/大纲/风格草案验收后交付。当用户要求制作、规划或改进 PPT/演示文稿/幻灯片（presentation / deck / slides）时使用本 skill。
whenToUse: 用户提出制作 PPT、演示文稿、幻灯片、deck，或要求规划 PPT 制作流程时。关键词：PPT、幻灯片、演示文稿、presentation、slides、deck、路演、汇报、答辩。
---

# PPT 制作全流程（门控式）

## 核心原则

1. **门控推进**：五个阶段、三道确认门。每道门必须获得用户**明确确认**后才能进入下一阶段，不得默认通过。
2. **先问后做**：开始任何制作前，必须先完成需求澄清，禁止直接动手做页。
3. **美术素材必备**：用户必须提供**一张主视觉 + 数张核心人物立绘**，PPT 的视觉风格只能从这些素材中提取，禁止凭空设计风格；数据、文字等参考素材按需提供。
4. **事实可信**：数据与事实只能来自用户提供的素材，禁止虚构。缺失素材默认用占位符标注并请用户补充。
5. **验证先行**：批量制作前必须通过 1-2 页验证页确认风格与版式。
6. **规划与制作分离**：本 skill 只做需求、风格、大纲、规格与验收，**不直接生成演示文件**；文件制作委托外部能力（见「制作委托」）。

## 阶段总览

```
Phase 0 需求澄清与风格提取 ──门A──▶ Phase 1 大纲与整体需求 ──门B──▶ Phase 2 验证页(1-2页) ──门C──▶ Phase 3 整体制作 ──▶ Phase 4 QA与交付
```

- **门 A**：需求清单 + 风格草案确认（含素材缺口清单）
- **门 B**：大纲与视觉规范确认
- **门 C**：验证页风格确认（可迭代修改后再确认）

## 审美增强（结合本机已有的创作类 skill）

制作与素材处理委托外部能力时，加载本机已安装的创作类 skill 提升审美与素材质量（执行时用 skill 工具加载，按其指导操作）：

| 场景 | 加载的 skill | 用途 |
|---|---|---|
| Phase 1 视觉规范细化 | `hyperframes-creative` | 配色体系、字体搭配、排版组合模式、品牌/风格决策——把风格草案扩展成可执行的视觉系统 |
| Phase 2/3 素材处理 | `media-use` | 立绘去背、图片统一调色、图标/装饰元素生成、Logo 处理、图片增强 |
| Phase 3 逐页版式 | `hyperframes-creative` | 每页布局对照其构图/组合模式，避免版式平庸 |
| 仅当以 HTML 形式制作或预览 | `hyperframes` / `hyperframes-core` / `hyperframes-animation` | HTML 幻灯片合成与动效（PPTX 交付则不用） |
| Phase 4 审美 QA | `hyperframes-creative` | 以设计规范视角复查：信息层级、节奏、留白、一致性 |

注意：这些 skill 面向视频/HTML 合成，取用其**设计方法论**（配色、排版、节奏、素材处理）；PPTX 交付时其动画与 HTML 合成能力不适用，不要生搬硬套。

## 制作委托（本 skill 不直接制作文件）

ppt-production 只负责规划、规格与验收，**不直接生成演示文件**。制作环节按以下顺序委托：

1. **PPTX 交付（默认）→ 委托 `ppt-master` 工作流**（已安装于 `vendor/ppt-master/`，仓库 hugohe3/ppt-master，48k+ star）：
   - 执行前先读取并遵守其 `SKILL.md`（注意其硬规则：以绝对路径记为 `SKILL_DIR`、先运行 `scripts/attribution_guard.py` 完整性校验、只读路由选定的 workflow，如 `workflows/generate-pptx.md` / `workflows/create-template.md` / `workflows/profiles/image-to-pptx.md` 等）；
   - 依赖：Python 3.10+，依赖装在 `vendor/ppt-master/.venv`（用 `.venv/bin/python3` 及 `.venv/bin/pip`）；
   - 工作目录：本工作区（project 会生成在 `<工作区>/projects/<名称>/`）；产出按委托书放到 `deliverables/`；
   - 优先使用其 **Create Template** 路由把已确认的风格草案建模为可复用模板，再用 Generate 生成整套 deck；
   - ppt-master 自带 ⛔ BLOCKING 门控，与本 skill 门控并行执行，两者都以用户明确确认为准。
2. **降级方案**：若 ppt-master 不可用（依赖缺失/校验失败），退回委托 agent 通用能力（bash + Python / python-pptx）制作；再不可用则降级 HTML 方案并告知用户（PPTX 需求不能静默降级）。
3. **HTML / 幻灯片交付或预览**：委托 `hyperframes` 系列 skill（`hyperframes` / `hyperframes-core` / `hyperframes-animation`）制作 HTML 幻灯片；仅当用户接受 HTML/PDF 形式时使用。
4. **素材处理**：委托 `media-use`（立绘去背、统一调色、图标/装饰元素、Logo 处理）。
5. 每次委托必须向执行方提供：**制作规格**（页面清单、视觉规范、素材路径、输出路径与格式）与**验收标准**；不接受偏离规格的产出，不合格退回修改。

文档与文件约定：需求/大纲 → `docs/`；素材 → `assets/`；成品 → `deliverables/`。

---

## Phase 0：需求澄清与风格提取

### 第 0 步：主题对象确认（最先做）

收到"制作 xxx 的 PPT"时，先确认 **xxx 是什么主题对象（领域）**——是游戏、小说、产品，还是其他话题——因为它决定后续提问的内容与方向：

1. 若用户连主题都没说：先问"要做什么主题的 PPT"，再确认领域。
2. 若主题已说但领域不明：用 `ask_user_question` 给出常见领域选项：**游戏 / 小说 / 产品服务 / 公司项目 / 人物 / 学术主题 / 其他**，请用户选择。
3. 领域确认后，**按领域定制提问**（见下表），再进入通用必问清单。
4. 若用户说"随便 / 你决定"：给出基于最常见理解的领域建议并请用户确认，**不要自作主张跳过**。

**按领域定制的提问示例**：

| 领域 | 关键提问 |
|---|---|
| 游戏 | 游戏类型（RPG / 射击 / 休闲 / 策略…）、平台（PC / 主机 / 手游）、核心玩法、特色亮点、目标玩家、是否展示截图或玩法演示 |
| 小说 | 小说类型（玄幻 / 科幻 / 言情 / 悬疑…）、作者、主要剧情线、核心人物、主题思想、剧透限制 |
| 产品 / 服务 | 产品类型、目标用户、核心功能、卖点、竞品对比 |
| 公司 / 项目 | 行业、业务模式、规模、核心数据、里程碑 |
| 人物 | 身份、主要成就、代表作品、他人评价 |
| 学术主题 | 学科、研究对象、方法、结论、参考文献 |
| 其他 | 按该主题的常识提出最相关的 3-5 个问题 |

### 第 1 步：通用必问清单（领域确认后再问）

用 `ask_user_question` 向用户提问，**每轮不超过 4 个问题**（可多轮），优先给出选项 + 推荐项：

1. **用途/场景**：商务汇报 / 学术答辩 / 产品发布 / 销售提案 / 培训教学 / 融资路演 / 年终总结 / 其他
2. **受众**：谁听、什么背景（领导/客户/评审/学生/公众）
3. **页数**：明确数字或范围
4. **风格气质偏好**：简洁商务 / 科技感 / 学术严谨 / 创意活泼 / 高端大气 / 极简 / 其他（仅供参考，最终视觉风格以美术素材提取为准）
5. **配色与品牌**：品牌色、Logo、现有模板（可附色号或图片）
6. **语言**：中文 / 英文 / 双语
7. **格式与尺寸**：PPTX / PDF；16:9 / 4:3
8. **截止时间**与优先级
9. **核心内容**：必须传达的信息、必备章节、是否需要演讲备注（按领域细化：游戏的玩法亮点、小说的剧情主线、产品的卖点等）

### 第 2 步：素材要求（美术资料必备）

本工具的核心是"**根据用户提供的美术材料，制作风格贴近的 PPT**"，因此美术资料是**必备**的，必须包含：

- **一张主视觉**（Key Visual：主题的标志性画面，用于定色调与整体气质）
- **数张核心人物立绘**（≥ 2-3 张，用于定人物画风、线条与配色细节）

其他参考素材（推荐，按需提供）：参考 PPT / 模板、数据文件 / 图表、文字资料 / 大纲草稿、对标 / 竞品案例。

若用户未提供主视觉或人物立绘：**风格无法提取**，须列出缺口请用户补充后再继续；不得跳过提取凭空设计风格。

### 第 3 步：美术风格提取与确认（进入 Phase 1 前必做）

用视觉分析工具（如 `qwen_vision` / `describe_image`）**逐张分析**主视觉与核心人物立绘，按以下维度提取风格：

| 维度 | 提取内容 |
|---|---|
| 色彩体系 | 主色 / 辅助色 / 强调色 / 背景色的近似色号（hex），明暗与饱和度基调 |
| 绘画风格 | 二次元 / 写实 / 3D 渲染 / 水墨国风 / 赛璐璐 / 厚涂 / 扁平插画 / 像素风 等 |
| 构图与版式 | 居中 / 对称 / 对角线构图、留白程度、出血与边框、装饰纹理、角标 |
| 线条与形状 | 线条锐利 / 圆润、描边风格、几何装饰元素 |
| 质感与特效 | 噪点、光效、渐变、纸张纹理、金属质感 |
| 字体气质 | 标题字体的视觉气质（衬线 / 无衬线、锐利 / 圆润、手写感）——只提取"气质"以选择近似免费字体，不得直接复制受版权保护的字体文件 |
| 装饰元素 | 可复用的图形元素（徽章、分隔线、图标风格、Logo 处理方式） |

按 `templates/style-spec.md` 填写 → 保存为 `docs/style-spec.md`（风格草案），**回读给用户确认风格**：

- 用户确认 → 风格定稿，进入 Phase 1（视觉规范以该草案为基础扩展）。
- 用户有调整（如改主色、换气质、加装饰）→ 修改草案再确认，直到通过。

**产出**：按 `templates/requirements.md` 填写 → 保存为 `docs/requirements.md`，连同风格草案一起回读给用户确认（**门 A：需求清单 + 风格草案**）。素材缺口整理成清单请用户补充；若用户同意用占位符，需在清单中明确标注。

---

## Phase 1：大纲与整体需求（门 A → 门 B）

1. **叙事主线先行**（借鉴叙事法）：先定一句话核心信息，再定钩子（开场）、展开（问题 → 方案 → 证据 → 行动）、收尾（结论/行动号召）。
2. 按主线产出**页面清单**：页号、页面类型（封面/目录/内容/图表/结尾）、标题、每页要点（每条 ≤ 20 字）、所需素材、演讲备注。
3. 产出**视觉规范**：以已确认的 `docs/style-spec.md` 风格草案为基础，补充字体（中/英）、栅格与留白、图表风格、动效原则、页脚页码，合并进大纲。视觉规范细化时加载 `hyperframes-creative` 辅助配色与排版组合设计（见「审美增强」）。
4. 产出**素材清单**：每页素材与状态（已有/缺失/占位）。
5. 按 `templates/outline.md` 填写 → 保存为 `docs/outline.md`，回读给用户确认（门 B）。修改意见先并入大纲，再请用户确认。

**大纲未确认前，不得开始制作任何页面。**

---

## Phase 2：验证页（门 B → 门 C；本 skill 出规格，制作委托外部）

1. 产出**验证页制作规格**：选择 **1-2 页最具代表性**的页面（默认：封面 + 一页内容密集页；用户可指定其他页面），写明每页的完整描述——内容要点、布局结构、用到的素材（封面用**主视觉**为核心画面、人物页用对应**立绘**）、视觉规范要点（配色/字体/版式）。
2. **委托制作**：按「制作委托」节执行，产出**真实文件**（不是示意图）。
3. **验收**：对照验证页规格与 `docs/style-spec.md` 风格草案逐项检查（素材是否用对、配色/版式是否达标）；不合格退回重做。若发现风格偏差，先回到风格草案迭代，再更新规格。
4. 向用户说明设计选择（配色、版式、字体、信息层级），请用户确认（门 C）；有修改意见 → 更新规格 → 重新委托 → 再次确认，直到通过。验证页规格与成品应直接复用为整体制作的一部分，不返工。

---

## Phase 3：整体制作（本 skill 出委托书，制作委托外部）

1. 产出**整体制作规格（委托书）**：基于已确认大纲，汇总最终页面清单（页号/类型/标题/要点）、视觉规范（从风格草案扩展）、素材清单（指向 `assets/` 的具体文件）、输出路径与格式（`deliverables/`，16:9，PPTX）。
2. **委托制作**：按「制作委托」节执行；不逐页打断用户，进度节点（约每 5-10 页）向用户简报。
3. 中途发现大纲问题（某页无法成立、素材缺失）：停下来询问用户，不擅自改大纲；素材缺失按已确认的占位策略处理。

---

## Phase 4：QA 验收与交付（门 C → 交付验收）

1. **对照验收**（本 skill 主持）：拿到成品后，对照三份已确认文档逐项验收——需求清单（内容完整、数据真实）、大纲（页面结构一致、无遗漏）、风格草案（配色/字体/版式一致）；再按 `templates/qa-checklist.md` 逐项检查。
2. **反思式复查**：以"陌生观众"视角通读全篇，检查叙事逻辑是否顺畅、每页是否只传达一个核心要点；同时以 `hyperframes-creative` 的设计规范视角复查审美（信息层级、节奏、留白，见「审美增强」）。
3. 不合格 → 列问题清单退回委托方修改，修改后重新验收；合格 → 交付：文件路径（`deliverables/`）、格式说明、使用说明（含修改建议）。
4. 用户验收通过后才算完成；反馈问题进入修改循环。

---

## 规则与注意事项

- 每阶段结束必须获得用户**明确确认**；用户只说"继续"不视为对当前产出已确认。
- 用户明确要求跳过某道门：可以跳过，但必须在 `docs/requirements.md` 中记录"已跳过确认门"并提示风险。
- 数据、事实、图片必须来自用户素材；需 AI 生成配图时，先告知并征得用户同意。
- 不做侵权（盗用他人版式/图片）、虚假数据、敏感内容。
- 验证页确认前不要批量制作；大纲确认前不要制作任何页面；本 skill 不直接制作文件，制作一律走「制作委托」。
